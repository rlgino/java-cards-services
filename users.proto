syntax = "proto3";

package usersproto;
option go_package = "grpcServer/users";

message User {
  enum ItemStatus {
    INACTIVE = 0;
    ACTIVE = 1;
  }

  string id = 1;
  string name = 2;
  ItemStatus status = 3;
}

message SearchRequest {
  string user = 1;
}

service UserService {
  rpc SearchUser(SearchRequest) returns (User);
}